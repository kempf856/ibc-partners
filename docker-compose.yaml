services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ibcpartner
      POSTGRES_USER: ibcpartner
      POSTGRES_PASSWORD: ibcpartner
    ports:
        - "5444:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
